from itemadapter import ItemAdapter


class JessopsPipeline:
    def process_item(self, item, spider):
        # Process the item as per your requirements
        # This method is called for each item scraped by the spider
        return item
